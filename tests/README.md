# Pruebas

- `unit/`: módulos aislados y lógica pura.
- `integration/`: comportamiento que conecta varios módulos del dominio.

Ejecuta toda la suite con `npm test`. Conserva las rutas relativas en pruebas compiladas para que puedan correr directamente con el test runner de Node.
