# Supabase

`migrations/` es el historial oficial del esquema y de las políticas de seguridad. Agrega una migración nueva por cambio; no edites una migración ya aplicada.

Las funciones de borde viven en `functions/`. Los SQL exploratorios o históricos deben permanecer en `scripts/sql/legacy/`.
